name: Build and Push Docker Image

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: japtoo
          password: dckr_pat_PEaFHd8Srxstv_XdhTq0lZ3QHjg

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: |
            japtoo/kevin-devops-19:latest
            japtoo/kevin-devops-19:${{ github.sha }}
